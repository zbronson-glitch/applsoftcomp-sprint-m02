# Notes: 2d-data

## Dataset structure
This dataset contains two numeric columns: `x` and `y`.

## Key observations
The data appears as paired 2D coordinates, so the natural visualization is a scatter plot.

## Visualization strategy
I used a scatter plot with `x` on the horizontal axis and `y` on the vertical axis to show the spatial distribution of the measurements.

## AI usage
I used ChatGPT to help troubleshoot Git, Python package installation, and plotting commands while I completed the assignment.
EOF\
cat <<'EOF' > docs/notes-1d-multi-method.md
# Notes: 1d-multi-method-data

## Dataset structure
This dataset contains a categorical method column (`method`) and a numeric performance column (`AUCROC`).

## Key observations
The goal is to compare the distribution of AUCROC values across methods and highlight differences in performance.

## Visualization strategy
I used a boxplot grouped by method to compare distributions clearly and compactly.

## AI usage
I used ChatGPT to help troubleshoot Git, Python package installation, and plotting commands while I completed the assignment.
